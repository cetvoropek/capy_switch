Config = {}

Config = {
   
    inventory = 'ox_inventory',              -- options can be default, ox_inventory
    
    useprogressbar = false,             -- if u wanna use progressbar write function in client side
    
    switchItem = 'fullautoswitch',       --item name for switch
    
    unswitchcommand = 'r_switch',       --unswitch command name here
    
--animation

    animDict = 'missmic4',
    animName = 'michael_tux_fidget',
    animduration = 5                   --in seconds
    
}
